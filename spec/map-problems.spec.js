const {
  incrementNums,
  multiplyNums,
  extractPonyNames,
  makeNameSentence,
  getFullNames,
  makeFilmDetails,
  getMainDetails
} = require("../array-methods/map-problems/challenges");
const { expect } = require("chai");

describe("incrementNums", () => {
  it("", () => {});
});
